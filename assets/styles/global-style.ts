import {StyleSheet} from 'react-native';

export const globalStyle = StyleSheet.create({
  screenContainer: {
    backgroundColor: '#fff',
    flex: 1,
    paddingHorizontal: 20,
    marginLeft: 10,
    marginRight: 8,
  },
});
